BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Passenger] ADD [ancho] VARCHAR(20) NOT NULL CONSTRAINT [Passenger_ancho_df] DEFAULT '0';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
